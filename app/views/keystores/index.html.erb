<p id="notice"><%= notice %></p>

<h1>Keystores</h1>

<table>
  <thead>
  <tr>
    <th>Url</th>
    <th>User</th>
    <th colspan="3"></th>
  </tr>
  </thead>

  <tbody>
  <% @keystores.each do |keystore| %>
      <tr>
        <td><%= keystore.url %></td>
        <td><%= keystore.name %></td>
        <td><%= link_to 'Download', keystore.url %></td>
        <td><%= link_to 'Destroy', keystore, method: :delete, data: {confirm: 'Are you sure?'} %></td>
      </tr>
  <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Keystore', new_keystore_path %>
